import { defineModel } from '../define-model.ts';
import type { InferEntity } from '@mikro-orm/core';

// Generated by turbo gen db-entity
// Adjust properties and types to your needs
export const {{properCase name}}Model = defineModel('{{properCase name}}', (p) => ({
    // TODO: add your fields here, e.g.
    // title: p.string(),
}));

// Export top-level EntitySchema for explicit registry and discovery
export const {{properCase name}} = {{properCase name}}Model.entity;

// Usage of types elsewhere: {{properCase name}} and {{properCase name}}['id']
export type {{properCase name}} = InferEntity<typeof {{properCase name}}>;
